{
  "name": "mindful-touch-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd frontend && npx tauri dev",
    "build": "cd frontend && npx tauri build",
    "tauri": "cd frontend && npx tauri"
  },
  "dependencies": {
    "@tauri-apps/api": ">=2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": ">=2.0.0"
  }
}